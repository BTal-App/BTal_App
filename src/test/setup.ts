// Setup global para Vitest · se ejecuta una vez antes de cualquier test.
//
// Importa los matchers de jest-dom adaptados a Vitest · habilita
// expressions tipo `expect(el).toBeInTheDocument()` que son la
// gramática estándar de Testing Library.
import '@testing-library/jest-dom/vitest';
